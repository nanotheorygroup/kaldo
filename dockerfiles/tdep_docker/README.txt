This docker file will build and compile LAMMPS, TDEP, and Kaldo.
It will then run the test suit to make sure everything is working.

TDEP may need changes in the important_settings file. If you need to make changes follow it on TDEP github.
